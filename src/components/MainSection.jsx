export default function MainSection({ children }) {
  return (
    <main className="max-w-[1110px] mx-auto border mt-[28px] mb-[20px] flex flex-col gap-[28px] w-full">
      {children}
    </main>
  )
}
